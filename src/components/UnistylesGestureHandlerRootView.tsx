import { GestureHandlerRootView } from "react-native-gesture-handler";
import { withUnistyles } from "@/src/theme/unistyles";

export const UnistylesGestureHandlerRootView = withUnistyles(GestureHandlerRootView);
